# Bunkai TMS — Frontend Infrastructure

> Generated: 2026-08-14 by `/project-discovery` Phase 3 (Infrastructure Discovery).
> Method: reverse-engineered from `../upex-bunkai-tms` — `next.config.ts`, `components.json`, `tailwind.config.ts`, `tsconfig.json`, `package.json`, `app/**`. Target repo read-only.

---

## Build Configuration

| Item | Value | Evidence |
|---|---|---|
| Framework | Next.js `^15`, App Router (`app/` dir, route groups `(app)` / `(auth)`) | `next.config.ts`; `app/` tree |
| Bundler | Next.js default (Webpack) — no `--turbo` flag in `dev`/`build` scripts, no `turbopack` key in `next.config.ts` | `package.json` scripts |
| Output mode | Server-rendered (RSC) — no `output: 'export'` / `output: 'standalone'` set in `next.config.ts` | `next.config.ts` |
| TypeScript | `strict: true`, `jsx: "preserve"`, `moduleResolution: "bundler"`, path aliases via `paths` (no `baseUrl` bareword imports) | `tsconfig.json` |
| React | `^19` | `package.json` |
| `typedRoutes` | `true` | `next.config.ts` — typed `Link`/`router.push` targets |
| Images | `remotePatterns: []` — no external image domains whitelisted | `next.config.ts` |

### `next.config.ts` (verbatim key settings)

```ts
{
  reactStrictMode: true,
  outputFileTracingRoot: path.resolve(import.meta.dirname),
  typedRoutes: true,
  images: { remotePatterns: [] },
}
```

No custom webpack config, no rewrites/redirects/headers blocks, no `env` passthrough block found.

## Package Scripts (frontend-relevant)

| Script | Command |
|---|---|
| `dev` | `next dev` |
| `build` | `next build` |
| `start` | `next start` |
| `format:check` / `format:fix` | Prettier over `json,yml,yaml,css,scss,html` |
| `lint:check` / `lint:fix` | ESLint (`@antfu/eslint-config` + `@next/eslint-plugin-next`) |

## Client Environment Variables

| Var | Prefix | Notes |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | `NEXT_PUBLIC_*` | Browser-safe, Supabase project URL |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | `NEXT_PUBLIC_*` | Browser-safe anon key (see `backend.md` for the `.env.example` naming discrepancy vs. actual code) |
| `NEXT_PUBLIC_APP_URL` | `NEXT_PUBLIC_*` | Used for auth redirects / OAuth callback construction |

**Security check**: no secret-looking value under a `NEXT_PUBLIC_*` name was found (`grep -rh "NEXT_PUBLIC_"` over `app/`, `lib/` surfaced only the three vars above — all conventionally browser-safe for a Supabase anon-key setup). No red flags.

## Environment-Specific Values

| Environment | `NEXT_PUBLIC_APP_URL` | Source |
|---|---|---|
| Local | `http://localhost:3000` | `.env.example` |
| Staging | `https://staging-upexbunkai.vercel.app` | Carried over from this QA repo's `.agents/project.yaml` — not independently found in a target-repo config file (no `vercel.json`) |
| Production | `https://upexbunkai.vercel.app` | Same as above — carried over, not independently confirmed in-repo |

OAuth redirect URLs are enumerated per-environment as a `.env.example` comment (for Supabase dashboard configuration, not read at runtime by the app itself):
`http://localhost:3000/auth/callback`, `https://staging-upexbunkai.vercel.app/auth/callback`, `https://upexbunkai.vercel.app/auth/callback`.

## Static Assets

```
public/
app/
  apple-icon.png
  icon.png
  icon.svg
```

- Favicon/app icons present (`app/icon.png`, `app/icon.svg`, `app/apple-icon.png` — Next.js file-based metadata convention, not a `public/` icon).
- `public/openapi.json` is generated by `bun run openapi:gen` (see `backend.md`) — a build artifact living in the static-asset dir, not a hand-authored asset.
- No `robots.txt` / `sitemap.xml` found at the root of `app/` or `public/` in this pass — Discovery Gap (product is behind auth for most routes, so this may be intentional).
- No image-optimization `next/image` domain config beyond the empty `remotePatterns: []` (i.e., no remote images are currently permitted — likely all icons/avatars are local or unimplemented).

## Code Splitting Strategy

No explicit `dynamic(...)` or `React.lazy` usage found in a surface pass of `app/(auth)/login/**`. The login form itself uses `<Suspense>` boundaries around client components that call `useSearchParams()` — required by Next 15's static-render bailout rules, not a deliberate perf-splitting strategy. Broader `app/(app)/**` code-splitting patterns not exhaustively audited this pass — Discovery Gap.

## Bundle Size Notes

Not measured — no `@next/bundle-analyzer` or equivalent dependency found in `package.json`. Discovery Gap.

## Performance Configuration

| Concern | Status |
|---|---|
| Image optimization | Default Next.js `next/image`, no remote domains configured |
| Font optimization | No `next/font` usage found in a surface pass — Discovery Gap (not exhaustively grepped) |
| Prefetching | Default Next.js `<Link>` prefetch behavior, no explicit tuning found |
| Script optimization | No `next/script` custom loading strategy found in a surface pass |

## SEO Configuration

Not found — no `metadata` export audit performed beyond the login page (`app/(auth)/login/page.tsx` has no explicit `export const metadata`). Discovery Gap; low relevance given the product is an internal, mostly-authenticated TMS tool.

## Browser Support / Polyfills

No `browserslist` entry found in `package.json`. Next.js 15 default target (modern evergreen browsers) applies. No explicit polyfill config found.

## Routing + State + Auth Integration Points

*(consumed by `/adapt-framework` when wiring KATA Page components and fixtures)*

| Concern | Finding |
|---|---|
| Router | Next.js App Router, route groups `(app)` (authenticated) and `(auth)` (public) |
| Protected routes | `middleware.ts` gates `/projects` and `/onboarding` prefixes — redirect-on-unauthenticated with a `next=` return-path query param |
| Public routes | `/login`, `/auth`, `/api/auth` (per `middleware.ts` `PUBLIC_PREFIXES`) |
| State management | No dedicated global-state library (Redux/Zustand/Jotai/Recoil) found in `package.json` — likely React Server Components + local `useState` (confirmed pattern in `EmailFirstForm`) |
| Data fetching | No TanStack Query / SWR / Apollo found — direct `fetch()` calls to `/api/v1/**` from client components (confirmed pattern in `EmailFirstForm`), RSC server-side data fetching elsewhere (not exhaustively audited) |
| Auth client | Supabase SSR cookie session (`@supabase/ssr`) — see `backend.md` §Auth Flow for the full multi-step login sequence |
| **Test IDs strategy** | **`data-testid` is the confirmed, actively-used convention.** 48 files under `app/` + `components/` contain `data-testid` attributes. Login form alone exposes: `login-email`, `login-password`, `login-continue`, `login-signin`, `login-create`, `login-otp`, `login-verify`, `login-resend`, `login-error`. This is the selector strategy `/adapt-framework` should wire KATA Page components against — prefer `data-testid` over CSS/text selectors everywhere it exists. |
| UI kit | `shadcn`-style (`components.json`, `style: "new-york"`, base color `neutral`), Radix UI primitives, Tailwind CSS `^3.4`, `lucide-react` icons |
| Editor component | Monaco Editor (`@monaco-editor/react`) — likely the ATC/step authoring surface; interacting with it in E2E tests may need Monaco-specific handling (not a plain `<input>`) |

## Discovery Gaps

- Font optimization, code-splitting depth, bundle size, and SEO metadata were only surface-checked against the login page — not exhaustively audited across `app/(app)/**`.
- No `robots.txt`/`sitemap.xml` found — unconfirmed whether intentional (auth-gated product).
- Environment URLs for staging/production are carried over from this QA repo's `.agents/project.yaml`, not independently re-derived from a target-repo config file (no `vercel.json` exists).
- Full `data-testid` inventory beyond the login flow (e.g. ATC editor, Run execution screens) not enumerated this pass — 48 files confirmed to use the convention, but per-page selector catalogs are a `/adapt-framework` or `/test-automation` task, not this discovery pass.

# Test Session Memory — BK-23

**TMS Modality:** jira-native (no Xray)
**Ticket Key:** BK-23
**Epic Key:** BK-13
**Jira Status (at session start):** Ready For QA
**Acceptance Criteria Count:** 4 (original, Jira-synced) — Stage 1 should also weigh the richer refined Gherkin set proposed in the 2026-06-02 Shift-Left QA comment (see `context.md` Team Discussion)

## Environment
- WEB_URL_OVERRIDE: none
- API_URL_OVERRIDE: none
- Active env: staging (`{{environments.staging.web_url}}` per `.agents/project.yaml`) — reachability already confirmed earlier this session (HTTP 307 on root). Not re-probed in this dispatch.

## Session Log
- 2026-06-22 — Session Start executed. Re-synced BK-23 from Jira (`bun run jira:sync-issues pull --story BK-23 --include-comments`); status moved from cached "Ready For Dev" to "Ready For QA". Created `module-context.md` (EPIC-BK-13, was missing) and `context.md` (this story, was missing) via backend code exploration of `upex-bunkai-tms`. Found no duplicate/clone implementation in the explored backend code despite Jira bot comments claiming a merged PR — flagged as an open question, not treated as a hard blocker yet.
- 2026-06-22 — User chose to verify the code-vs-Jira discrepancy live on staging before Stage 1. Dispatched a Playwright probe: staging magic-link login is broken — the OTP email has no matching code-entry field on the "Check your inbox" screen (confirmed via empty `document.querySelectorAll('input')`, reproduced twice). Could not reach the ATC library; the original duplicate-ATC question remains empirically unanswered.
- 2026-06-22 — Filed **BK-175** ("Auth: Login: Magic-link OTP email has no code-entry field on staging", Error Type=Functional, Severity=Crítica, Test Environment=Staging) with full repro + technical analysis + evidence screenshot attached. Created issuelink **BK-175 Blocks BK-23** (verified direction via `acli jira workitem link list --key BK-175 --json` → `outwardIssueKey: BK-23`). Posted a QA-paused comment on BK-23 (no status transition — ticket stays "Ready For QA", nothing about the feature itself was disproven, the environment just couldn't be reached).
- **SESSION PAUSED** — blocked by BK-175. Resume condition: BK-175 is fixed/closed AND dev/PO confirms BK-23's deployment status on staging. Do not start Stage 1 Planning until both are true.
- 2026-06-22 — User asked whether the local backend clone (`upex-bunkai-tms`) might simply be stale. Confirmed: local `staging` was 180 commits behind `origin/staging` (`54749ba` vs `16863ca`). Ran `git fetch` + `git stash` (preserved an unrelated local `opencode.jsonc` MCP-config edit) + `git pull` + `git stash pop` — local now matches `origin/staging` at `16863ca`. This resolved the original "PR merged but no code found" discrepancy: BK-23's duplicate-ATC code is real and merged (`5f02be9` merge of `feature/BK-23-atc-duplicate`, `80a4fe6 feat(BK-23): duplicate ATC with steps, assertions and AC bindings`) — the bot comment was correct, the gap was only in the stale local clone.
- 2026-06-22 — Investigated whether BK-166 (merged ~30min after the original failed staging probe, `16863ca`, adds an email+password login as the new primary method, magic-link demoted to a collapsed secondary option) could provide a workaround login path. Live-tested staging (`https://staging-upexbunkai.vercel.app/login`): the password-flow UI is **not yet deployed** there — only the old magic-link-only screen renders. Re-confirmed BK-175 reproduces identically a third time.
- 2026-06-22 — Pulled the actual OTP email via `resend emails receiving get` against the test inbox (`bunkai-staging-userbunk@olkacoraug.resend.app`) and found the precise root cause: Supabase Auth's email template for this flow sends an 8-digit code with **no link at all** (only an unrelated "Opt out" link), while `magic-link-form.tsx` is built purely for a click-the-link flow and renders no code-entry input. Also found a secondary copy bug in the same email: text says "6-digit code" but the actual code is 8 digits. Posted this full root-cause analysis (with suggested fix directions) as a comment on BK-175.

## Notable discovery (orchestrator-level, not story-specific)
`.agents/jira-fields.json` is **stale** relative to the live Jira workspace — several custom-field IDs no longer match their documented slugs (e.g. the catalog's `actual_result` → `customfield_10133` actually resolves to "In Regression Plan (QA)📊" today; `evidence` → `customfield_10140` actually resolves to "Change risk (migrated)"). Confirmed empirically via `/rest/api/3/issue/createmeta/BK/issuetypes/10021` and `/rest/api/3/field` while creating BK-175 — the real IDs are: error_type=`customfield_10141`, severity=`customfield_10143`, test_environment=`customfield_10057`, actual_result=`customfield_10056`, expected_result=`customfield_10059`, evidence=`customfield_10061`, root_cause=`customfield_10109`, fix=`customfield_10107`. Likely caused by the two Jira site migrations referenced in several status descriptions ("Migrated on 14 Apr 2026" / "Migrated on 3 Jun 2026"). Recommend running whatever `jira:sync-fields`-equivalent script regenerates `.agents/jira-fields.json`, or a manual re-sync, before the next bug filing that depends on this catalog.
